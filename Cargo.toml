[workspace]
resolver = "2"
members = [
    "crates/rapidfem-core",
    "crates/rapidfem-fd",
    "crates/rapidfem-td",
    "python",
]
# Vendored dependencies carry their own workspace (vendor/rslab).
exclude = ["vendor"]

[profile.release]
opt-level = 3
lto = true
